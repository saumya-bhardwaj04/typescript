# Any, Unknown and Never
- any: turns off type checking
- unknown: uses type guard(condition checking) and type narrowing(refining the type) for type check.
- never: For functions that will never return a value